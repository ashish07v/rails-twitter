<%= form_with(model: user, local: true, url: url_add) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Topic %>
    <%= form.text_field :topic_name %>
  </div> 

  <div class="actions">
    <%= form.submit button_name %>
  </div>
<% end %>
