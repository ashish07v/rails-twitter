<h1> Create Topic </h1>

<%= render 'topic', user: @topic , url_add: create_topic_user_path, button_name: 'Create Topic'%>
<table>
  <thead>
    <tr>
      <th>Topic name</th>
      <th > action</th>
    </tr>
  </thead>

  <tbody>
<% # = render 'all_topic', topic: @my_topics %>
<% @my_topics.each do |review| %>
<tr>
        <td><%= review.topic_name %></td>
        <td></td>    
      </tr>

<% end %>
</tbody>
</table>

<%= link_to 'Back', twitter_path %>